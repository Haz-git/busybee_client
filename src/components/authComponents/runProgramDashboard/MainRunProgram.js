//This component should be immediately rendered after clicking 'Confirm' on program run.
import React from 'react';

//Styles:
import styled from 'styled-components';

const MainContainer = styled.div``;

const MainRunProgram = () => {
    return <MainContainer>The component should be running here;</MainContainer>;
};

export default MainRunProgram;
