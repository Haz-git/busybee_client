import React from 'react';
import styled from 'styled-components';

//Render:

const PrimaryStatCard = () => {
    return <>This should be the primary stat card.</>;
};

export default PrimaryStatCard;
